#!/usr/bin/env bash
case $2 in
spoof)
  ifconfig $1 | grep ether | cut -d' ' -f2 > mac-address.txt
  addr=$(openssl rand -hex 6 | sed 's/\(..\)/\1:/g; s/.$//')
  echo "Setting $1's mac to $addr"
  ;;
restore)
  addr=$(cat mac-address.txt)
  ;;
*)
  echo "Usage: $0 interface command"
  ;;
esac
