#!/bin/bash
# Create webm files from avi files (or any file, really).

# Encode to webm (vpx, vorbis) at 1400kb/s with a constant rate factor of 9.
# Overwrite files if needed. Delete the original file if successful.
ffmpeg -y -i "$1" -b:v 1400k -crf 9 "${1%%.*}.webm" && rm -v "$1"
