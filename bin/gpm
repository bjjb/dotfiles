#!/usr/bin/env python3
from gmusicapi import Musicmanager

mm = Musicmanager()
mm.login()
uploaded_songs = mm.get_uploaded_songs()
print("You have %d uploads" % len(uploaded_songs))
purchased_songs = mm.get_purchased_songs()
print("You have %s purchases" % len(purchased_songs))
