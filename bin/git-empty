#! /bin/sh
# Copyright (c) 2014, JJ Buckley

die () {
  printf >&2 '%s\n' "$*"
  exit 1
}

branch=$1
if test "$branch" = ""
then
  die "You need to give the empty branch a name"
fi

shift
if test $# != 0
then
  die "Usage: $0 <branch>"
fi

git checkout --orphan $branch
git rm -rqf .
